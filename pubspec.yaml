name: interoperabilidade
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=2.2.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  archive: ^2.0.10
  path_provider: ^1.1.0
  cloud_firestore: ^0.12.5+2
  cupertino_icons: ^0.1.2  
  firebase_core: ^0.4.0+6
  

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
   - assets/sticker_packs.wasticker
   - assets/WAStickersPack/webp/
   - assets/WAStickersPack/png/

